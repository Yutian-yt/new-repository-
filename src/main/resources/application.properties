spring.sql.init.mode=never
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=040209

# ==========================================
# 2. ?? Redis ??
# ==========================================
# Redis?????
spring.redis.host=localhost
# Redis????? (??6379)
spring.redis.port=6379
# Redis?? (?????????????????)
# spring.redis.password=
# ??????
spring.redis.timeout=5000ms
# ????? (??????)
spring.redis.lettuce.pool.max-active=8
spring.redis.lettuce.pool.max-idle=8
spring.redis.lettuce.pool.min-idle=0

# ==========================================
# 3. ?? MyBatis-Plus ??
# ==========================================
# ?? Mapper XML ?????
# classpath*:mapper/*.xml ???? resources/mapper ?????? xml ??
mybatis-plus.mapper-locations=classpath*:mapper/*.xml
# ???????? (? user_name -> userName)
mybatis-plus.configuration.map-underscore-to-camel-case=true
# ?? SQL ?????? (????????)
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# JWT ??
security.jwt.secret=demo-jwt-secret-key-for-spring-security-2026
security.jwt.expiration-millis=7200000

# ==========================================
# 4. 大模型配置 (DashScope)
# ==========================================
# DashScope API Key - 通过环境变量读取，避免硬编码到代码仓库
spring.ai.dashscope.api-key=${AI_DASHSCOPE_API_KEY}
# 配置默认使用的模型
spring.ai.dashscope.chat.model=qwen-turbo
# 请求超时时间
spring.ai.dashscope.chat.timeout=60000